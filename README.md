# Node Gr Cli

This is a Cli to generate [NodeGr](https://www.npmjs.com/package/@node-gr/core) project


### How to
install the cli package globally

`npm install -g @node-gr/cli`

then run

`gr new <ProjectName>`
